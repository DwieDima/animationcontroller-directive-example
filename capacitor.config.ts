import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'animation-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
