import { NgIfAnimatedDirective } from './ng-if-animated.directive';

describe('NgIfAnimatedDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfAnimatedDirective();
    expect(directive).toBeTruthy();
  });
});
